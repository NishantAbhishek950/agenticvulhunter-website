# AgenticVulHunter website

Static website for AgenticVulHunter. No build step is required.

## Preview locally

```bash
python3 -m http.server 8081
```

Then open `http://localhost:8081`.

## GitHub Pages

Commit the contents of this folder to the root of your website repository, then enable GitHub Pages for the `main` branch and `/ (root)`.
